# วิธี Deploy ขึ้น Railway (ไม่ต้องแก้โค้ดเลย)

โปรเจกต์นี้ถูกออกแบบให้รันเป็น **process เดียวที่เปิดค้างไว้ตลอด** พร้อมเก็บข้อมูล/รูปสลิปลงดิสก์ตรงๆ
ซึ่ง **Vercel ไม่รองรับรูปแบบนี้** (serverless = ไม่มี process ค้าง, ดิสก์ไม่ persist) — Railway รองรับแบบนี้โดยตรง จึงไม่ต้องแก้โค้ดสักบรรทัด

สิ่งที่เพิ่มให้แล้วในโปรเจกต์:
- `package.json` — ให้ Railway รู้จักและรันได้ด้วย `npm start`
- `.gitignore` — กันไม่ให้ไฟล์ข้อมูล/รูปสลิปในเครื่องเราหลุดขึ้น git (ของจริงจะอยู่ใน Volume บน Railway แทน)

## ขั้นตอน

### 1) เอาโค้ดขึ้น GitHub
ถ้ายังไม่มี repo:
```bash
cd lamatodmatodmala
git init
git add .
git commit -m "initial commit"
```
แล้วสร้าง repo ใหม่บน GitHub (private ได้) แล้ว push ขึ้นไปตามคำสั่งที่ GitHub ให้มา

### 2) สร้างโปรเจกต์บน Railway
1. ไปที่ https://railway.app แล้วสมัคร/ล็อกอินด้วย GitHub
2. กด **New Project** → **Deploy from GitHub repo** → เลือก repo นี้
3. Railway จะตรวจเจอ `package.json` และรัน `npm start` ให้อัตโนมัติ (ใช้ Nixpacks)
4. ตัวแปร `PORT` — Railway ใส่ให้เองอัตโนมัติ และโค้ด (`process.env.PORT || 3000`) รองรับอยู่แล้ว ไม่ต้องตั้งอะไรเพิ่ม

### 3) เพิ่ม Volume (สำคัญมาก — ห้ามข้าม!)
ถ้าไม่ทำขั้นนี้ ข้อมูลออเดอร์/สต็อก/รูปสลิปจะ**หายทุกครั้งที่ deploy ใหม่หรือ container restart** เพราะดิสก์ของ container เป็นแบบชั่วคราว

1. เข้า service ที่ deploy ไว้ → แท็บ **Settings** → หา **Volumes**
2. กด **+ New Volume**
3. ตั้ง **Mount Path** เป็น `/app/data`
   (โค้ดสร้างโฟลเดอร์ `data/` เทียบจากตำแหน่งไฟล์ `server.js` เอง — บน Railway โปรเจกต์จะถูกวางไว้ที่ `/app` พอดี)
4. ขนาดเริ่มต้นเล็กๆ ก็พอ (รูปสลิปเป็น JPG/PNG ไม่กี่ร้อย KB ต่อรูป) ขยายทีหลังได้

> Volume ใช้ได้ตั้งแต่แพลน **Hobby ($5/เดือน)** ขึ้นไป — แพลน Free ของ Railway ให้เครดิตแค่ ~$1/เดือน ไม่พอสำหรับรันตลอดเวลาแบบ production จริงจัง แนะนำอัปเป็น Hobby เลยเพื่อไม่ให้แอปหลับ/โดนตัด

### 4) เปิด public URL
1. แท็บ **Settings** → **Networking** → กด **Generate Domain**
2. จะได้ URL แบบ `xxxx.up.railway.app` ใช้งานได้ทันที
3. (ถ้ามีโดเมนตัวเอง) เพิ่ม **Custom Domain** แล้วชี้ CNAME ตามที่ Railway บอก

### 5) ทดสอบ
- เปิด URL ที่ได้ → ลองสั่งของ 1 ออเดอร์ พร้อมแนบรูปสลิป
- เข้าหลังบ้าน (PIN ตั้งต้น `0607` — ไปเปลี่ยนในแท็บตั้งค่าร้านได้) เช็คว่า:
  - ✅ popup แจ้งเตือนขึ้นพร้อมเสียง
  - ✅ รูปสลิปขึ้นในออเดอร์
  - ✅ redeploy โปรเจกต์ 1 ครั้ง (กด Deploy ใหม่จาก Railway) แล้วเช็คว่าออเดอร์เก่ายังอยู่ครบ (ยืนยันว่า Volume ทำงาน)

### 6) ปิดของเดิมบน Vercel
เพื่อไม่ให้ลูกค้าหลงเข้าไปหน้าเก่าที่บั๊ก ให้เข้า Vercel dashboard → ลบ/ปิดโปรเจกต์เดิม หรือปรับ DNS ให้ชี้มาที่ Railway แทน

## เรื่องรองรับคนพร้อมกันเยอะๆ
Node.js เป็น event loop เดียวที่จัดการ connection พร้อมกันได้เยอะมากอยู่แล้วโดยธรรมชาติ (ไม่ block ต่อ request) — งานเบาๆ แบบนี้ (อ่าน/เขียน JSON เล็กๆ, ส่ง SSE) รองรับคนพร้อมกันได้หลักร้อยถึงหลักพัน connection สบายๆ ด้วยแพลน Hobby ($5) ทั่วไปอยู่แล้ว ถ้าร้านโตขึ้นมากจริงๆ ค่อยขยับไปแพลน Pro ของ Railway (เพิ่ม RAM/CPU) ได้ทีหลัง ไม่ต้องแก้โค้ด
